#ifndef FUNCS_H
#define FUNCS_H

class Funcs
{
public:
    Funcs(/* args */);
    ~Funcs();
    static bool verifyCollision(int*, int*);
    static bool animation(int, int, int*, int*, bool);
};

#endif